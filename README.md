# dotfiles

run 🚀

```bash
sudo apt install unzip && \
    wget -O main.zip https://github.com/ratushnyivm/dotfiles/archive/refs/heads/main.zip && \
    unzip main.zip -d dotfiles && \
    cd dotfiles/dotfiles-main && \
    sh main.sh && \
    sudo rm -rf main.zip
```
